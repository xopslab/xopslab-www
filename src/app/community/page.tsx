import {Button} from "@/components/ui/button";
import {FaDiscord, FaYoutube, FaTwitter, FaLinkedin} from "react-icons/fa";
import {Input} from "@/components/ui/input";
import {Metadata} from "next";
import Image from "next/image";
import {Card, CardContent, CardDescription, CardHeader, CardTitle} from "@/components/ui/card";
import UnderConstructionHero from "@/components/UnderConstructionHero";

export const metadata: Metadata = {
  title: "Community | XOps Lab",
  description: "Join our community of AI, ML, and Cloud enthusiasts.",
};

export default function CommunityPage() {
  return (
    <>
    <UnderConstructionHero/>
    </>
  );
}
